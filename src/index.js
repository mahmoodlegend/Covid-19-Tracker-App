import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root')); 
            
            // ReactDOM and on render method on it

// ReactDOM take two parameters an App and a document index in public folder